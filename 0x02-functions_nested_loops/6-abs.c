#include "main.h"
/**
 * _abs - Computes absolute value of an integer
 * @m: Integer input
 * Return: Absolute value of integer
 */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	else
	{
		return (m * -1);
	}
}
